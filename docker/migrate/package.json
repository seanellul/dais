{
  "name": "dais-migrate",
  "version": "0.1.0",
  "private": true,
  "description": "Boot-time migration helper for the Dais Docker image. Versions are pinned to the ones the root pnpm-lock.yaml resolves; bump them together and regenerate package-lock.json with `npm install --package-lock-only`.",
  "type": "module",
  "dependencies": {
    "@electric-sql/pglite": "0.5.8",
    "drizzle-orm": "0.45.2",
    "pg": "8.23.0"
  }
}
